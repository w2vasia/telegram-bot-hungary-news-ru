services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./bot:/app/bot
      - ./tests:/app/tests
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
